# PhotoReview — Audit từng function và plan tối ưu

Cập nhật 2026-09-15, baseline `5189298`. Bốn lane audit bằng subagent: UI/interaction; cache, hash, sort; journal/native/recovery; tests/tools/docs. Đây là plan thực hiện, chưa phải tuyên bố các fix đã triển khai. Mức P1: có thể sai ảnh/sai file/sai trạng thái hoặc mất an toàn; P2: tác động đáng kể tới tốc độ/độ bền; P3: tối ưu nhỏ. Mỗi task cần fixture và test hành vi tương ứng trước khi đổi DONE.

### Evidence implementation đợt 1 — 2026-09-15

FA-01 đã có guard generation cho scan lỗi và xử lý phím folder/Undo khi không có ảnh; FA-02 đã từ chối operation lạ và lưu Skip ngay. FA-06 đã chống waiter cancellation/clear/dispose làm refill ThumbnailCache và sửa quota accounting. FA-07 đã giữ shared hash tới khi underlying task xong, kiểm tra fingerprint sau hash và có generation khi Clear. FA-08 đã dùng temp path unique và canonical folder key cho SessionStore. FA-09/12 đã sửa natural ordering numeric run dài, comparer hash, compare pair cùng folder/extension và ignored-count drag-drop. Contract runner Release PASS; GUI/fault/benchmark gates vẫn mở.

## Thứ tự thực hiện và dependency

| Đợt | Task | Phụ thuộc | Kết quả bắt buộc |
|---|---|---|---|
| 0 — Baseline | FA-00 fixture, benchmark, UI/fault harness | — | Manifest fixture, số đo P50/P95 key-to-visible, source reads/bytes, RAM/GC, test crash boundary |
| 1 — Đúng trạng thái | FA-01 folder/presentation identity; FA-02 action serialization; FA-03 undo/batch safety | FA-00 | Không stale UI, không thao tác nhầm file, không xóa hết bản duplicate |
| 2 — Đúng recovery | FA-04 journal identity/torn record; FA-05 staged Move/Copy/Recycle/restore | FA-02 | Reconcile không xác nhận nhầm destination cùng size, retry/restore không overwrite |
| 3 — Đúng cache | FA-06 preview key/cancellation; FA-07 hash freshness; FA-08 session concurrency | FA-01, FA-00 | Không trả preview/hash cũ, clear không bị refill, session JSON bền |
| 4 — Tốc độ | FA-09 scan/sort metadata; FA-10 preload/COM worker; FA-11 disk cache quota; FA-12 UI layout/zoom | FA-01, FA-06, FA-00 | P95 và số lần đọc được đo trên cùng fixture; không hồi quy chất lượng |
| 5 — Gate | FA-13 GUI/Explorer matrix; FA-14 release docs | FA-01..12 | Test/build/publish/remote commit và báo cáo giới hạn đã chứng minh |

## UI và state — `MainWindow.xaml.cs`

| Function(s) | Task | Finding/điểm chạm | Cách thực hiện và nghiệm thu |
|---|---|---|---|
| constructor, `OpenFolder_Click`, `Window_Drop` | FA-01 | Các folder load task bị bỏ không quan sát; constructor reconcile trên UI [MainWindow:51,65,88](../../PhotoReview.App/MainWindow.xaml.cs) | Một hàm OpenFolder async có ownership; khởi tạo sau Loaded; lỗi được báo một lần. Fixture mở A chậm rồi B, chỉ B còn trên UI. |
| `LoadFolderAsync` | FA-01/09 | Scan không nhận token, catch load cũ có thể ghi đè UI B; tính bytes sau Explorer, `long.MaxValue` chặn preload full-folder lần đầu [MainWindow:113,122,162](../../PhotoReview.App/MainWindow.xaml.cs) | Catalog snapshot có generation/token; mọi write/catch kiểm tra identity. Một pass metadata dùng cho size/total. Test A lỗi sau khi mở B; folder dưới 16 GB bắt đầu preload toàn bộ khi phù hợp. |
| `ShowImageAsync`, compare/status/session updates | FA-01 | Sau các await compare/hash/dimensions không kiểm tra presentation generation [MainWindow:201](../../PhotoReview.App/MainWindow.xaml.cs) | Mọi await và publish UI/session dùng cùng presentation ID. Rapid navigation từ pair chậm sang ảnh thường không để pair/status cũ. |
| `GetPreviewAsync`, `GetTargetDecodeWidth`, `GetDiskCachePath` | FA-06 | RAM/in-flight key chỉ theo path; width/mode/fingerprint không nằm trong key; decode có thể refill cache cũ [MainWindow:17,235,320](../../PhotoReview.App/MainWindow.xaml.cs) | Cache key theo source identity + mode + decode width + pipeline version; cancellation/generation chặn insert cũ. Resize/mode change hiển thị bitmap đúng độ nét. |
| `Settings_Click`, `UpdateFitSize`, `ApplyInitialViewMode` | FA-06/12 | Sau đổi LoadingMode/Fit, ảnh hiện tại không refresh; Fit resize dựa vào initial mode [MainWindow:92,614](../../PhotoReview.App/MainWindow.xaml.cs) | Invalidate variant bị ảnh hưởng, refresh current; track view mode hiện tại. Test Preview→Original, zoom→Fit→resize. |
| `GetOriginalDimensionsAsync`, `SetZoom` | FA-09/12 | Metadata/length đọc lại khi revisit và zoom [MainWindow:293,633](../../PhotoReview.App/MainWindow.xaml.cs) | Decode trả dimensions và catalog size; zoom không gọi `FileInfo`. Fixture đổi file in-place cập nhật metadata. |
| `PreloadAroundAsync`, `PreloadOneAsync`, `HasPreloadHeadroom` | FA-10 | Active decode vẫn chạy sau hủy; snapshot `_files.ToArray()` lặp, full-folder hai-task batches [MainWindow:327,365](../../PhotoReview.App/MainWindow.xaml.cs) | Một scheduler priority current→neighbors→folder, bounded 1/2/4 theo benchmark; cancel active request/chặn stale cache; resume khi memory pressure hạ. |
| `Window_KeyDown`, `Matches` | FA-02/12 | Guard `_index<0` chặn folder navigation/Undo khi folder rỗng; Enter/Delete có thể chạy lại; Skip chưa save ngay [MainWindow:401,428](../../PhotoReview.App/MainWindow.xaml.cs) | Folder/Undo xử lý trước image guard; file action lock theo source/session; skip lưu bền; modifier matching rõ. Test empty-folder PgDown, last-image Ctrl+Z, key-repeat. |
| `ClassifyCurrentAsync`, `ExecuteActionAsync` | FA-02/05 | File/journal I/O synchronous trên UI; unknown operation rơi vào Move; physical action có thể thành công rồi commit lỗi [MainWindow:652,691](../../PhotoReview.App/MainWindow.xaml.cs) | Capture source trước confirm, reject operation lạ, chạy I/O qua executor có state machine; reconcile outcome trước update catalog. Fault inject sau move/commit; không báo success giả. |
| `RemoveDuplicatesAsync`, click wrappers | FA-03 | Hash serial, catalog đổi khi await; “remove original” có thể chọn mọi file trong group cùng hash [MainWindow:476,488](../../PhotoReview.App/MainWindow.xaml.cs) | Freeze candidate + folder ID; bounded hash; mỗi hash group giữ ít nhất một survivor; dry-run cho biết survivor. Hai non-numbered copies còn một; đổi folder lúc hash không recycle folder khác. |
| `NavigateSiblingFolderAsync`, `FindNextImageFolder` | FA-01/10 | Search sibling không cancel và có thể mở folder cũ sau manual open [MainWindow:519,533](../../PhotoReview.App/MainWindow.xaml.cs) | Kiểm tra folder generation trước load; cache image-presence có invalidation. Test PgDown chậm rồi mở B thủ công. |
| `UndoLastMoveAsync`, `UndoLastActionAsync` | FA-03/05 | Undo history toàn cục thêm file A vào catalog/session B và sort lại không theo Explorer [MainWindow:731,751](../../PhotoReview.App/MainWindow.xaml.cs) | Undo physical đúng folder nguồn; reload/catalog source theo sort active, session B không đổi. Test A→B→Undo Move/Recycle. |
| `Window_Closed`, `Window_Closing`, `ExitFullscreen` | FA-01/12 | Task còn chạy khi dispose; fullscreen exit luôn Maximized [MainWindow:565,604](../../PhotoReview.App/MainWindow.xaml.cs) | Shutdown flag và observe active work; save/restore normal bounds. Đóng giữa scan/decode không unobserved fault; F11/Escape về vị trí cũ. |
| `DecodeWithFallback`, `ClearCache_Click`, compare selection handlers | FA-06/12 | Full-res fallback cho mọi scaled error; clear chưa phủ in-flight/preview disk; compare Tag có thể stale [MainWindow:307,449,462](../../PhotoReview.App/MainWindow.xaml.cs) | Chỉ fallback lỗi codec xác định, cache clear có scope/epoch, compare pair typed identity. Test corrupt huge file, clear-during-decode, pair transition click. |
| `DecodeSource`, `WindowHandle.Handle`, `Diagnostics_Click`, `IsSamePath` và helper thuần | — | Audit không thấy defect riêng có bằng chứng. | Giữ behavior, chỉ test regression khi đổi caller. |

## Cache, hash, sort, session

| Function(s) | Task | Finding | Cách thực hiện và nghiệm thu |
|---|---|---|---|
| `ThumbnailCache.GetAsync/AwaitAndCacheAsync/ClearMemory/Dispose` | FA-06 | Một waiter hủy làm remove shared in-flight dù decode còn chạy; clear có thể refill; mỗi RAM hit vẫn probe filesystem [ThumbnailCache:40,57](../../PhotoReview.App/ThumbnailCache.cs) | Remove in-flight khi underlying task kết thúc; epoch cache; reject call sau dispose. Test hủy một waiter nhưng chỉ một decode, clear khi decode, RAM hit I/O counter. |
| `ThumbnailCache.BuildKey/DecodeAsync/EstimateBytes` | FA-06/10 | Stamp size+mtime có thể miss edit cùng metadata; WPF decode synchronous không hủy giữa chừng; estimate cố định 4 B/pixel [ThumbnailCache:125,164](../../PhotoReview.App/ThumbnailCache.cs) | Chính sách freshness explicit, hậu-decode token check, đo footprint theo pixel format/GC. Fixture rewrite cùng size/mtime và ảnh lớn. |
| `ThumbnailCache.WriteAtomicallyAsync/PruneDiskCache/ClearDisk/TryDelete` | FA-11 | Prune toàn directory mỗi write, trừ size dù delete fail; write có thể hoàn thành sau clear [ThumbnailCache:94,105,144](../../PhotoReview.App/ThumbnailCache.cs) | Batch serialized quota scan, verify delete rồi mới trừ, bound writes, cache epoch khi clear. Test nhiều cache entries, locked oldest, clear-during-write, không còn `.tmp`. |
| `BoundedLruCache.Set` | FA-06 | Oversized replacement để lại value cũ cùng key [BoundedLruCache:41](../../PhotoReview.App/BoundedLruCache.cs) | Chốt replacement policy; test old key→oversized new value. Constructor/Count/TryGet/Remove/Clear không có defect riêng. |
| `FileHashService.GetAsync/ComputeAndCacheAsync/Clear` | FA-07 | In-flight theo path, waiter hủy remove task còn chạy; cache dùng pre-read metadata dù file có thể đổi giữa hash [FileHashService:15,28](../../PhotoReview.App/FileHashService.cs) | Key shared work theo fingerprint; remove sau completion; hậu-read verify metadata hoặc từ chối cache, clear epoch. Test mutate mid-read và canceled waiter. |
| `ImageSortService.Sort/GetFileSize/NaturalKey/ExplorerComparer` | FA-09 | Size mode đọc metadata riêng rồi folder tính total lại; natural key pad 12; comparer Equals/GetHashCode khác contract [ImageSortService:10,20,26,44](../../PhotoReview.App/ImageSortService.cs) | Catalog metadata một pass, numeric-run comparer không fixed width, ordinal tie-breaker; tách comparer order khỏi equality. Test 13-digit names, inaccessible file, size probe count, hash collection contract. |
| `AdaptivePreviewPolicy.CalculateTargetDecodeWidth` | FA-10/12 | Min 1200/max 4000 có thể thừa decode viewport nhỏ hoặc mờ zoom/DPI cao [AdaptivePreviewPolicy:9](../../PhotoReview.App/AdaptivePreviewPolicy.cs) | Tune dựa image dimensions, zoom và measured latency/quality; test DPI/zoom boundary. `Validate` giữ nguyên. |
| `DragDropInputService.Parse/Normalize`, `ImageFileTypes.IsSupported` | FA-12 | Mixed folder/duplicate input báo `IgnoredPathCount` chưa thống nhất [DragDropInputService:26](../../PhotoReview.App/DragDropInputService.cs) | Định nghĩa count là input không dùng, normalize mỗi path một lần; test mixed/invalid/relative. Extension registry hiện nhất quán trong app. |
| `ComparePairService.Find` | FA-12 | Scan toàn list và match stem có thể ghép khác folder/extension [ComparePairService:8](../../PhotoReview.App/ComparePairService.cs) | Index stems theo folder/extension policy; test hai folder trùng stem và repeated lookup. |
| `SessionStore.Load/Save/GetPath` | FA-08 | Save cùng `.tmp` khi concurrent; load tin folder trong JSON; path tương đương Windows hash khác [SessionStore:19,27,36](../../PhotoReview.App/SessionStore.cs) | Per-instance stable root, normalized folder key, unique temp + serialized save; validate loaded folder. Test parallel saves và casing/trailing separator. |
| `ReviewMetrics.Record*/Snapshot` | FA-00 | Diagnostic counters có thể nhận số âm; snapshot nhiều counter không atomic chung [ReviewMetrics:15](../../PhotoReview.App/ReviewMetrics.cs) | Validate non-negative, ghi rõ snapshot gần đúng; bổ sung mốc key-to-visible đo bằng GUI harness. Các atomic increment cơ bản giữ nguyên. |

## Settings, dialogs và tooling

| Function(s)/file | Task | Finding | Cách thực hiện và nghiệm thu |
|---|---|---|---|
| `AppSettings.Load/Save/Migrate` | FA-08 | `Load` catch mọi lỗi rồi cố backup và ghi default; `Save` dùng `config.json.tmp` cố định [AppSettings:21,51](../../PhotoReview.App/AppSettings.cs) | Tách corrupt JSON khỏi I/O permission, không overwrite config unreadable; unique temp + durable replace. Test locked/readonly config, concurrent save, migration backup failure. |
| `AppSettings.NormalizeLoadingMode/NormalizeImageSortMode/IsValidImageSortMode/ValidateShortcuts` | FA-12 | Unknown loading mode chuyển Fast; PortraitFirst cũ chuyển Name; validator sort lặp Name và chưa kiểm tra operation/destination đầy đủ [AppSettings:75,88,98,105](../../PhotoReview.App/AppSettings.cs) | Chốt migration/invalid-value policy, nhất quán UI/runtime; test schema v1, unknown mode, shortcut conflict và action invalid. |
| `SettingsWindow.Save_Click/Defaults_Click/ShortcutText_PreviewKeyDown` | FA-12 | Editor nhận một key, không chord; default reset không reset `Actions`; save lỗi I/O không có UI error rõ [SettingsWindow:74,80,116](../../PhotoReview.App/SettingsWindow.xaml.cs) | Rõ phạm vi modifier, reset đầy đủ hoặc ghi label, catch save failure; UI test reset/action JSON/readonly config. |
| `ActionProfilesWindow.Import_Click/Export_Click/Apply_Click` | FA-12 | Import kiểm tra non-empty nhưng validate operation/destination cuối chưa đầy đủ; export ghi trực tiếp file [ActionProfilesWindow:38,51,57](../../PhotoReview.App/ActionProfilesWindow.xaml.cs) | Dùng chung validator với Settings, atomic export/no-overwrite policy theo UI; test JSON operation lạ/đích thiếu/ghi fail. |
| `RecoveryWindow.Retry_Click`, `BatchReviewWindow.Confirm_Click` | FA-05/03 | Dialog là tuyến xác nhận cuối của mutation nhưng chưa có GUI acceptance về focus, selection và dry-run survivor [RecoveryWindow:21](../../PhotoReview.App/RecoveryWindow.xaml.cs) | Fixture UI keyboard-only, click repeat và danh sách source/destination before/after. |
| `AppLog.Write`, `PhysicalMemory.HasHeadroom`, `InstanceLock.Dispose`, `WindowPlacementService` | FA-10/12 | Diagnostics, memory-pressure và instance lifecycle cần đo/verify hành vi dưới lỗi/đóng app, chưa có finding khác đủ bằng chứng [AppLog:14](../../PhotoReview.App/AppLog.cs) | Fault fixture cho readonly log root, memory pressure và repeated startup/shutdown; chỉ sửa nếu test chỉ ra lỗi. |
| `tools/benchmark-folder.ps1` | FA-00 | Chỉ đo sequential disk read, có webp ngoài registry; không đo WPF key-to-frame [benchmark-folder:1](../../tools/benchmark-folder.ps1) | Đổi filter theo registry hoặc tách rõ storage-only; GUI benchmark ghi P50/P95 + read counters trên fixture. |
| `tools/verify-all.ps1/verify-release.ps1` | FA-14 | Default verify `outputs/release` khác publish path bắt buộc; không chứng minh artifact từ HEAD [verify-all:1](../../tools/verify-all.ps1) | Một gate gọi đúng Release test + publish path AGENTS, provenance HEAD/build stamp, verifier fail-fast; test thiếu artifact. |
| `tools/smoke-test.ps1/fault-injection-test.ps1` và `PhotoReview.Tests/Program.cs` | FA-00/05/13 | Smoke/fault scripts dùng fixture riêng; nhiều contract test check chuỗi source, không chạy app executor/crash [Program:25](../../PhotoReview.Tests/Program.cs) | Thêm injectable executor + fixture state matrix, WPF UI automation; giữ source-wiring check chỉ với vai trò wiring. |

## Filesystem, recovery và native

| Function(s) | Task | Finding | Cách thực hiện và nghiệm thu |
|---|---|---|---|
| `OperationJournal.Append/ReadEntries/ReadPendingOperations` | FA-04 | Crash có thể để JSONL cuối bị cắt; parser bỏ qua lặng lẽ [OperationJournal:14,53](../../PhotoReview.App/OperationJournal.cs) | Phát hiện/report torn record; schema v2 có stronger identity và state. Crash injection trước/giữa/sau append. |
| `OperationJournal.ReconcilePendingOperations` | FA-04 | Move Committed khi source mất và destination cùng length, có thể xác nhận file không liên quan [OperationJournal:84](../../PhotoReview.App/OperationJournal.cs) | Destination identity/hash/fingerprint phù hợp policy I/O; nếu không chắc thì `NeedsReview`, không tự Committed. Test unrelated same-size destination. |
| `RecoveryRetryService.RetryMoveOrCopy` | FA-05 | Source chỉ size+mtime, Copy lỗi có thể để destination chặn retry [RecoveryRetryService:9](../../PhotoReview.App/RecoveryRetryService.cs) | Staged copy, source recheck và explicit partial state; không xóa file chưa xác nhận ownership. Test source replaced, partial copy, commit-write failure. |
| `RecycleBinRestoreService.TryRestore/WaitForRestore` | FA-05 | Match path+size, không dùng expected mtime; occupied original path có thể báo restore success giả [RecycleBinRestoreService:10,53](../../PhotoReview.App/RecycleBinRestoreService.cs) | Refuse occupied path, identify recycle item và verify size/mtime sau verb. Test same path/size older items, occupied destination, failed restore. |
| `ExplorerOrderService.TryGetSnapshotAsync/QueryShell/TryReadNativeView` | FA-10/13 | Timeout chỉ dừng await; mỗi call tạo STA thread, hung COM tích lũy; failed group query thành None; một returned pointer path cần release mọi HRESULT [ExplorerOrderService:21,53,103](../../PhotoReview.App/ExplorerOrderService.cs) | Dedicated bounded STA worker, clear ownership COM/pointer, Unknown grouping. Test repeated blocked provider; probe Name/Date/Size/group. |
| `ExplorerSnapshotValidator.TryValidate` | FA-13 | Malformed path có thể throw thay vì fallback [ExplorerOrderService:148](../../PhotoReview.App/ExplorerOrderService.cs) | Return reason trên mọi invalid native path; test malformed/duplicate/missing/extra non-image policy. |
| `WindowPlacementService.Restore/Save` | FA-12 | Native SetWindowPlacement fail chưa báo; shared `.tmp` có thể conflict [WindowPlacementService:24,43](../../PhotoReview.App/WindowPlacementService.cs) | Check Win32 return, unique temp và saved normal bounds; test concurrent instance/disconnected monitor. |
| `ExplorerComInterop`, `SiblingFolderService` và helper native đơn giản | FA-13 | Không phát hiện defect vtable slot bằng static audit. | Native integration trên OS đích; không đổi slot theo suy đoán. |

## Cách đóng mỗi task

1. Ghi fixture/OS/build và baseline trước thay đổi, đặc biệt số source reads, P50/P95 visible frame, RAM peak, correctness checks.
2. Implement trên scope function đã nêu; không tối ưu bằng cách giảm chất lượng ảnh mặc định trái `AGENTS.md`.
3. Test hành vi/fixture thật tương ứng và regression test hiện có; chạy Release tests, publish đúng path `AGENTS.md`, verify artifact.
4. Cập nhật plan/status bằng evidence và commit/push `origin/master` theo quy trình project. Nếu gate GUI/benchmark chưa chạy, ghi `NOT_TESTED` thay vì DONE.
